// FlowBoxes removed per user request (Selected Work). Keep empty component to avoid import errors if any remain.
import React from 'react'

const FlowBoxes: React.FC = () => null

export default FlowBoxes
